#include <bits/stdc++.h>

using namespace std;

int NTHH(char c) {
    if(c == 'H') return 1;
    if(c == 'O') return 16;
    if(c == 'C') return 12;
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // freopen("CHO.INP", "r", stdin);
    // freopen("CHO.OUT", "w", stdout);
    string s; cin >> s;
    stack<char> st;
    bool check = false;
    int s = 0;
    for(char c : s) {

    }

    return 0;
}
