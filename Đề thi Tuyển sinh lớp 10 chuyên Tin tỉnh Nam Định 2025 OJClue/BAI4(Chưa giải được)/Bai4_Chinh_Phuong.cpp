#include <bits/stdc++.h>

using namespace std;

bool cP(int n) {
    int t = sqrt(n);
    return t*t == n;
}

int main() {   
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
   
    return 0;
}
